# ☁️ Размещение сервера в облаке (как Discord)

Чтобы приложение работало как Discord (без ввода IP адресов), нужно разместить сервер в облаке.

## 🚀 Способ 1: Railway (РЕКОМЕНДУЕТСЯ - самый простой)

### Шаг 1: Регистрация

1. Перейдите на https://railway.app/
2. Войдите через GitHub (бесплатно)

### Шаг 2: Создание проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Подключите ваш репозиторий (или создайте новый)

### Шаг 3: Настройка

1. Railway автоматически определит Node.js проект
2. В настройках проекта:
   - **Start Command:** `node server-cloud.js`
   - **Port:** Railway автоматически установит переменную `PORT`

### Шаг 4: Получение адреса

1. После деплоя Railway даст вам адрес (например: `voice-chat-production.up.railway.app`)
2. Скопируйте этот адрес

### Шаг 5: Обновление приложения

Измените в `public/app.js` адрес сервера на ваш Railway адрес (см. инструкцию ниже)

---

## 🌐 Способ 2: Render (бесплатно)

### Шаг 1: Регистрация

1. Перейдите на https://render.com/
2. Зарегистрируйтесь через GitHub

### Шаг 2: Создание Web Service

1. Нажмите "New +" → "Web Service"
2. Подключите ваш репозиторий
3. Настройки:
   - **Name:** voice-chat-server
   - **Environment:** Node
   - **Build Command:** `npm install`
   - **Start Command:** `node server-cloud.js`
   - **Plan:** Free

### Шаг 3: Деплой

1. Нажмите "Create Web Service"
2. Дождитесь деплоя
3. Получите адрес (например: `voice-chat.onrender.com`)

---

## 📝 Обновление клиента для использования облачного сервера

После размещения сервера в облаке нужно обновить клиент:

### Вариант 1: Изменить код (для EXE)

В файле `public/app.js` найдите строку:
```javascript
const serverIP = serverIpInput ? (serverIpInput.value.trim() || 'localhost') : 'localhost';
```

Замените на:
```javascript
// Адрес вашего облачного сервера
const CLOUD_SERVER = 'ваш-адрес.railway.app'; // или .onrender.com

const serverIP = serverIpInput ? (serverIpInput.value.trim() || CLOUD_SERVER) : CLOUD_SERVER;
```

### Вариант 2: Сделать поле IP опциональным

Можно скрыть поле IP и всегда использовать облачный сервер.

---

## 🎯 После настройки:

1. **Все пользователи** подключаются к одному адресу
2. **Не нужно вводить IP** - приложение автоматически подключается к облачному серверу
3. **Работает из любой сети** - как Discord!

---

## 📦 Файлы для деплоя

Для деплоя нужны файлы:
- `server-cloud.js` - облачная версия сервера
- `package.json` - зависимости
- `.gitignore` - игнорируемые файлы

**Не нужно:**
- `main.js` (только для Electron)
- `public/` (если не нужен веб-интерфейс)

---

## ✅ Быстрая инструкция Railway:

1. Зарегистрируйтесь на https://railway.app/
2. Создайте проект из GitHub репозитория
3. Установите Start Command: `node server-cloud.js`
4. Скопируйте адрес
5. Обновите `public/app.js` с новым адресом
6. Пересоберите EXE: `npm run build-win`
7. Готово! 🎉

---

## 💡 Преимущества:

- ✅ Работает из любой сети
- ✅ Не нужно вводить IP адреса
- ✅ Постоянный адрес (не меняется)
- ✅ Бесплатно (на Railway/Render)
- ✅ Работает как Discord!


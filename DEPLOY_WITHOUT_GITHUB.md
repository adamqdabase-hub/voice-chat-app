# 🚀 Деплой без GitHub репозитория

Если у вас нет репозитория на GitHub, есть несколько способов:

## Способ 1: Создать репозиторий на GitHub (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Создайте репозиторий

1. Перейдите на https://github.com/
2. Войдите в аккаунт (или создайте новый)
3. Нажмите "+" в правом верхнем углу → "New repository"
4. Название: `voice-chat-app` (или любое другое)
5. Выберите "Public" или "Private"
6. **НЕ** отмечайте "Initialize with README"
7. Нажмите "Create repository"

### Шаг 2: Загрузите код

В PowerShell в папке проекта выполните:

```powershell
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/voice-chat-app.git

# Загрузите код
git branch -M main
git push -u origin main
```

**Если Git не установлен:**
- Скачайте с https://git-scm.com/download/win
- Установите
- Перезапустите PowerShell

### Шаг 3: Деплой на Railway

Теперь вернитесь на Railway и выберите ваш новый репозиторий.

---

## Способ 2: Railway CLI (без GitHub)

### Шаг 1: Установите Railway CLI

```powershell
# Через npm
npm install -g @railway/cli

# Или скачайте с https://railway.app/cli
```

### Шаг 2: Войдите в Railway

```powershell
railway login
```

### Шаг 3: Создайте проект

```powershell
cd C:\Users\Adam\Downloads\гс
railway init
```

### Шаг 4: Деплой

```powershell
railway up
```

Railway автоматически загрузит файлы и задеплоит.

---

## Способ 3: Render (проще, без GitHub)

### Шаг 1: Регистрация

1. Перейдите на https://render.com/
2. Зарегистрируйтесь

### Шаг 2: Создайте Web Service

1. Нажмите "New +" → "Web Service"
2. Выберите "Build and deploy from a Git repository"
3. Подключите GitHub (если нужно) или используйте "Public Git repository"

**Или используйте "Manual Deploy":**
1. Нажмите "New +" → "Web Service"
2. Выберите "Manual Deploy"
3. Загрузите файлы проекта

### Шаг 3: Настройки

- **Name:** voice-chat-server
- **Environment:** Node
- **Build Command:** `npm install`
- **Start Command:** `node server-cloud.js`
- **Plan:** Free

---

## Способ 4: Простое решение - используйте готовый сервер

Если не хотите настраивать деплой, можно использовать временный сервер или попросить друга, у которого есть сервер.

---

## 🎯 Рекомендация

**Самый простой способ:** Создайте репозиторий на GitHub (5 минут) и используйте Railway.

**Быстрый способ без GitHub:** Используйте Railway CLI.

---

## 📝 Файлы для деплоя

Убедитесь, что в проекте есть:
- ✅ `server-cloud.js` - облачный сервер
- ✅ `package.json` - зависимости
- ✅ `.gitignore` - игнорируемые файлы

**Не нужны для деплоя:**
- ❌ `main.js` (только для Electron)
- ❌ `public/` (если не нужен веб-интерфейс)
- ❌ `node_modules/` (установится автоматически)


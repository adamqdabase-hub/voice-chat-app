# 📥 Инструкция по установке и сборке

## Шаг 1: Установка Node.js

Node.js включает в себя npm (менеджер пакетов), поэтому вам нужно установить только Node.js.

### Скачайте Node.js:

1. Перейдите на официальный сайт: https://nodejs.org/
2. Скачайте **LTS версию** (рекомендуется) для Windows
3. Запустите установщик и следуйте инструкциям
4. **Важно:** Убедитесь, что при установке отмечена опция "Add to PATH"

### Проверка установки:

Откройте командную строку (PowerShell или CMD) и выполните:

```bash
node --version
npm --version
```

Если вы видите версии (например, v20.x.x и 10.x.x), значит всё установлено правильно!

---

## Шаг 2: Установка зависимостей проекта

1. Откройте командную строку в папке проекта
2. Выполните команду:

```bash
npm install
```

Это установит все необходимые библиотеки (Express, Socket.io, Electron и др.)

---

## Шаг 3: Запуск приложения для тестирования

После установки зависимостей вы можете запустить приложение:

```bash
npm start
```

Приложение откроется в отдельном окне.

---

## Шаг 4: Сборка EXE файла

Для создания установщика (.exe) выполните:

```bash
npm run build-win
```

Или просто:

```bash
npm run build
```

### Где найти готовый EXE:

После сборки готовый установщик будет находиться в папке:
```
dist/Голосовой Чат Setup X.X.X.exe
```

Вы можете распространять этот файл друзьям - им не нужно устанавливать Node.js!

---

## 🎯 Быстрая справка команд:

- `npm install` - установить зависимости (выполнить один раз)
- `npm start` - запустить приложение для тестирования
- `npm run build-win` - создать EXE установщик
- `npm run dev` - запустить с открытой консолью разработчика

---

## ❓ Частые проблемы:

### 🔴 Ошибка 2502/2503 при установке Node.js

Эта ошибка обычно связана с правами доступа. Решения:

**Решение 1: Запуск от имени администратора (РЕКОМЕНДУЕТСЯ)**

**Способ А - Через PowerShell от имени администратора:**
1. Нажмите `Win + X`
2. Выберите **"Терминал Windows (Администратор)"** или **"Windows PowerShell (Администратор)"**
3. Нажмите "Да" в окне UAC
4. Выполните команды:
   ```powershell
   cd C:\Users\Adam\Downloads
   .\node-v24.11.1-x64.msi
   ```
5. Следуйте инструкциям установщика

**Способ Б - Просто двойной клик:**
1. Двойной клик на файл `node-v24.11.1-x64.msi`
2. Когда появится окно UAC - нажмите **"Да"**
3. Следуйте инструкциям установщика

**Решение 2: Очистка временных файлов**
1. Нажмите `Win + R`
2. Введите: `%temp%` и нажмите Enter
3. Удалите все файлы в папке (можно выделить всё `Ctrl+A` и удалить)
4. Попробуйте установить Node.js снова

**Решение 3: Отключение антивируса на время установки**
1. Временно отключите антивирус
2. Запустите установщик от имени администратора
3. После установки включите антивирус обратно

**Решение 4: Использование ZIP версии (без установщика)**
1. Скачайте ZIP версию Node.js с https://nodejs.org/
2. Распакуйте в папку (например, `C:\nodejs`)
3. Добавьте путь вручную в переменные окружения:
   - `Win + Pause` → "Дополнительные параметры системы"
   - "Переменные среды" → "Path" → "Изменить"
   - Добавьте путь к папке Node.js (например, `C:\nodejs`)

**Ошибка "npm не является внутренней или внешней командой":**
- Node.js не установлен или не добавлен в PATH
- Переустановите Node.js с опцией "Add to PATH"

**Ошибка при сборке:**
- Убедитесь, что все зависимости установлены: `npm install`
- Проверьте, что у вас достаточно места на диске

**Приложение не запускается:**
- Убедитесь, что порт 3000 свободен
- Закройте другие приложения, использующие этот порт

---

## 📝 Примечания:

- Для разработки нужен Node.js
- Готовый EXE файл работает **без установки Node.js** на других компьютерах
- EXE файл включает в себя всё необходимое (Electron, сервер, зависимости)


# 🌐 Настройка для работы через интернет

Для работы через интернет (когда вы и друг в разных сетях) нужно настроить туннелирование сервера.

## 🚀 Способ 1: Использование ngrok (САМЫЙ ПРОСТОЙ)

### Шаг 1: Установка ngrok

1. Перейдите на https://ngrok.com/
2. Зарегистрируйтесь (бесплатно)
3. Скачайте ngrok для Windows
4. Распакуйте в любую папку

### Шаг 2: Настройка

1. Запустите ваше приложение:
   ```powershell
   npm start
   ```

2. В **новом** окне PowerShell запустите ngrok:
   ```powershell
   ngrok http 3000
   ```

3. Ngrok покажет что-то вроде:
   ```
   Forwarding  https://abc123.ngrok.io -> http://localhost:3000
   ```

4. Скопируйте адрес (например: `https://abc123.ngrok.io`)

### Шаг 3: Использование

**Вы (создатель комнаты):**
1. Запустите приложение
2. Создайте комнату
3. Сообщите другу: **ngrok адрес** (например: `abc123.ngrok.io`) и **ID комнаты**

**Друг:**
1. Запустите приложение
2. В поле "IP сервера" введите ngrok адрес (например: `abc123.ngrok.io`)
3. Введите ID комнаты
4. Нажмите "Присоединиться"

**Важно:** Ngrok бесплатный адрес меняется при каждом запуске. Если перезапустите ngrok - получите новый адрес.

---

## 🔧 Способ 2: Использование Cloudflare Tunnel (бесплатно, постоянный адрес)

### Установка Cloudflare Tunnel:

1. Перейдите на https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/
2. Следуйте инструкциям по установке `cloudflared`

### Использование:

```powershell
cloudflared tunnel --url http://localhost:3000
```

Получите постоянный адрес для вашего сервера.

---

## 🌍 Способ 3: Настройка роутера (для постоянного решения)

Если у вас есть доступ к роутеру:

1. **Настройте проброс портов:**
   - Зайдите в настройки роутера (обычно `192.168.1.1`)
   - Найдите "Проброс портов" или "Port Forwarding"
   - Добавьте правило: порт 3000 → IP вашего компьютера

2. **Узнайте ваш внешний IP:**
   - Перейдите на https://whatismyipaddress.com/
   - Скопируйте IP адрес

3. **Используйте внешний IP:**
   - Друг должен ввести ваш внешний IP в поле "IP сервера"

**⚠️ Внимание:** Это открывает ваш компьютер в интернет. Используйте только если понимаете риски.

---

## 📝 Рекомендации

**Для тестирования:** Используйте ngrok (Способ 1) - самый простой

**Для постоянного использования:** Cloudflare Tunnel (Способ 2) - бесплатно и надежно

**Для продвинутых:** Настройка роутера (Способ 3) - требует знаний

---

## ✅ Что уже настроено в приложении

Приложение уже настроено для работы через интернет:
- ✅ Используются публичные STUN серверы
- ✅ Используются публичные TURN серверы (для обхода NAT)
- ✅ Поддержка подключения по IP адресу или домену

Вам нужно только настроить туннелирование сервера (ngrok или другой способ).

---

## 🎯 Быстрая инструкция с ngrok:

1. Установите ngrok с https://ngrok.com/
2. Запустите приложение: `npm start`
3. В другом окне: `ngrok http 3000`
4. Скопируйте адрес (например: `abc123.ngrok.io`)
5. Друг вводит этот адрес в поле "IP сервера"
6. Готово! 🎉


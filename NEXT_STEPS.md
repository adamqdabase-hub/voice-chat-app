# ✅ Что делать дальше после успешного деплоя

## Шаг 1: Получите адрес сервера

1. В Railway нажмите на сервис **"voice-chat-app"**
2. Перейдите на вкладку **"Settings"** (справа)
3. Найдите раздел **"Networking"** или **"Domains"**
4. Railway автоматически создал домен, например:
   ```
   voice-chat-app-production.up.railway.app
   ```
   или
   ```
   voice-chat-app.railway.app
   ```
5. **Скопируйте этот адрес** (без https://)

---

## Шаг 2: Проверьте работу сервера

Откройте адрес в браузере (например: `https://voice-chat-app-production.up.railway.app`)

Должно появиться:
```json
{
  "status": "online",
  "message": "Voice Chat Server is running",
  "rooms": 0
}
```

Если видите это - сервер работает! ✅

---

## Шаг 3: Обновите приложение

1. Откройте файл `public/app.js` в редакторе

2. Найдите строку **81**:
   ```javascript
   // const CLOUD_SERVER = 'ваш-сервер.railway.app'; // или .onrender.com
   ```

3. Раскомментируйте и укажите ваш адрес:
   ```javascript
   const CLOUD_SERVER = 'voice-chat-app-production.up.railway.app'; // ваш реальный адрес
   ```

4. Найдите строку **85**:
   ```javascript
   // Если настроен облачный сервер, раскомментируйте:
   // defaultServer = CLOUD_SERVER;
   ```

5. Раскомментируйте:
   ```javascript
   defaultServer = CLOUD_SERVER;
   ```

---

## Шаг 4: Загрузите обновление в GitHub

```powershell
cd C:\Users\Adam\Downloads\гс

git add public/app.js
git commit -m "Update cloud server address"
git push
```

Railway автоматически перезапустит сервис (если нужно).

---

## Шаг 5: Пересоберите EXE для друзей

```powershell
npm run build-win
```

Готовый EXE будет в папке `dist/`

---

## Шаг 6: Используйте приложение

1. Запустите приложение (`npm start` или EXE)
2. Введите имя
3. Нажмите "Создать новую комнату"
4. Скопируйте ID комнаты
5. Поделитесь **только ID комнаты** с друзьями
6. Друзья вводят ID и присоединяются
7. **Не нужно вводить IP адреса!** 🎉

---

## ✅ Готово!

Теперь приложение работает как Discord:
- ✅ Все подключаются к одному серверу
- ✅ Не нужно вводить IP адреса
- ✅ Работает из любой сети
- ✅ Постоянный адрес

---

## 🐛 Если что-то не работает:

1. Проверьте, что адрес сервера правильный
2. Проверьте, что сервер работает (откройте в браузере)
3. Проверьте консоль приложения (F12) на ошибки
4. Убедитесь, что обновили `public/app.js` и пересобрали EXE


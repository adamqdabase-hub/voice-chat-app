# ⚡ Быстрая настройка GitHub репозитория

## Шаг 1: Установите Git (если еще не установлен)

1. Скачайте с https://git-scm.com/download/win
2. Установите (все настройки по умолчанию)
3. Перезапустите PowerShell

## Шаг 2: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/
2. Войдите (или создайте аккаунт)
3. Нажмите **"+"** → **"New repository"**
4. Название: `voice-chat-app`
5. Выберите **"Public"**
6. **НЕ** отмечайте галочки
7. Нажмите **"Create repository"**

## Шаг 3: Загрузите код

В PowerShell в папке проекта (`C:\Users\Adam\Downloads\гс`):

```powershell
# Инициализируйте git
git init

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Initial commit"

# Добавьте удаленный репозиторий
# ЗАМЕНИТЕ YOUR_USERNAME на ваш GitHub username!
git remote add origin https://github.com/YOUR_USERNAME/voice-chat-app.git

# Загрузите код
git branch -M main
git push -u origin main
```

**При первом push Git попросит авторизацию:**
- Используйте Personal Access Token (см. ниже)

## Шаг 4: Создайте Personal Access Token

1. На GitHub: Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите "Generate new token"
3. Название: `railway-deploy`
4. Отметьте `repo` (все галочки в разделе repo)
5. Нажмите "Generate token"
6. **Скопируйте токен** (показывается только один раз!)

При push используйте:
- Username: ваш GitHub username
- Password: токен (не пароль!)

## Шаг 5: Деплой на Railway

1. Вернитесь на Railway
2. Выберите ваш репозиторий `voice-chat-app`
3. Railway автоматически начнет деплой
4. В настройках установите Start Command: `node server-cloud.js`
5. Дождитесь завершения
6. Скопируйте адрес сервера

## ✅ Готово!

Теперь у вас есть:
- ✅ Репозиторий на GitHub
- ✅ Сервер на Railway
- ✅ Постоянный адрес для подключения

---

## 🔧 Альтернатива: Railway CLI (без GitHub)

Если не хотите создавать репозиторий:

```powershell
# Установите Railway CLI
npm install -g @railway/cli

# Войдите
railway login

# В папке проекта
cd C:\Users\Adam\Downloads\гс
railway init
railway up
```

Готово! Railway загрузит файлы напрямую.


# 🚀 Быстрый запуск приложения

## Шаг 1: Проверка установки Node.js

Откройте **НОВЫЙ** PowerShell или командную строку (закройте старую и откройте новую!) и выполните:

```powershell
node --version
npm --version
```

### ❌ Если команды не работают:

**Вариант 1: Перезапустите терминал**
- Закройте все окна PowerShell/CMD
- Откройте новое окно PowerShell
- Попробуйте снова: `node --version`

**Вариант 2: Перезагрузите компьютер**
- После установки Node.js иногда нужно перезагрузить компьютер
- После перезагрузки откройте PowerShell и проверьте: `node --version`

**Вариант 3: Переустановите Node.js**
- Убедитесь, что при установке была отмечена галочка **"Add to PATH"**
- Переустановите Node.js, если галочка не была отмечена

---

## Шаг 2: Установка зависимостей

Когда `node --version` и `npm --version` работают:

1. Откройте PowerShell в папке проекта:
   - Перейдите в папку: `C:\Users\Adam\Downloads\гс`
   - Зажмите `Shift` и **правой кнопкой** на папке
   - Выберите **"Открыть окно PowerShell здесь"**

2. Выполните команду:
   ```powershell
   npm install
   ```
   
   Это займет 1-2 минуты. Дождитесь завершения.

---

## Шаг 3: Запуск приложения

После установки зависимостей выполните:

```powershell
npm start
```

Приложение откроется в отдельном окне! 🎉

---

## Шаг 4: Создание EXE файла (опционально)

Чтобы создать EXE файл для друзей:

```powershell
npm run build-win
```

Готовый EXE будет в папке `dist/`

---

## ❓ Проблемы?

**"npm не является внутренней или внешней командой":**
- Node.js не установлен или не в PATH
- Перезапустите терминал или перезагрузите компьютер
- Переустановите Node.js с галочкой "Add to PATH"

**Ошибки при `npm install`:**
- Убедитесь, что интернет подключен
- Попробуйте еще раз: `npm install`

**Приложение не запускается:**
- Убедитесь, что выполнили `npm install`
- Проверьте, что порт 3000 свободен




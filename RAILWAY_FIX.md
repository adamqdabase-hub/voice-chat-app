# 🔧 Исправление ошибки на Railway

## Проблема:

Railway пытается запустить Electron, но на сервере Linux Electron не работает. Нужно запускать `server-cloud.js`.

## ✅ Решение:

### В Railway:

1. Перейдите в **"Settings"** вашего сервиса
2. Найдите раздел **"Deploy"** или **"Configuration"**
3. Найдите поле **"Start Command"**
4. Измените с `npm start` на:
   ```
   node server-cloud.js
   ```
   или
   ```
   npm run server
   ```
5. Сохраните изменения
6. Railway автоматически перезапустит сервис

---

## Альтернатива: Через файл railway.json

Создайте файл `railway.json` в корне проекта:

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "node server-cloud.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

Или через переменную окружения:

1. В Railway Settings → Variables
2. Добавьте переменную:
   - **Name:** `NPM_START`
   - **Value:** `node server-cloud.js`

---

## ✅ После исправления:

1. Railway перезапустит сервис
2. Дождитесь статуса **"Running"**
3. Получите адрес сервера в Settings → Networking
4. Готово! 🎉


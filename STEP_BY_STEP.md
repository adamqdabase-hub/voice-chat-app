# 📋 Пошаговая инструкция

## Шаг 1: Загрузите исправление в GitHub

### 1.1. Откройте PowerShell в папке проекта

Нажмите `Win + X` → выберите **"Windows PowerShell"** или **"Терминал Windows"**

### 1.2. Перейдите в папку проекта

```powershell
cd C:\Users\Adam\Downloads\гс
```

### 1.3. Проверьте статус

```powershell
git status
```

Должны увидеть `server-cloud.js` в списке измененных файлов.

### 1.4. Добавьте файл

```powershell
git add server-cloud.js
```

### 1.5. Сделайте коммит

```powershell
git commit -m "Fix rooms variable declaration order"
```

### 1.6. Загрузите в GitHub

```powershell
git push
```

**Если попросит авторизацию:**
- Username: ваш GitHub username
- Password: используйте Personal Access Token (не пароль!)

---

## Шаг 2: Дождитесь деплоя (1-2 минуты)

### 2.1. Откройте Railway

Перейдите на https://railway.com/ и откройте ваш проект

### 2.2. Откройте сервис "voice-chat-app"

Нажмите на карточку сервиса

### 2.3. Следите за статусом

- Сначала будет **"Building"** (желтый)
- Потом **"Deploying"** (синий)
- В конце **"Running"** (зеленый) ✅

**Время ожидания:** 1-2 минуты

### 2.4. Проверьте логи (опционально)

Перейдите на вкладку **"Logs"** - должны увидеть:
```
Сервер запущен на порту 3000
Готов к подключениям!
```

---

## Шаг 3: Проверьте сервер в браузере

### 3.1. Откройте адрес сервера

В браузере откройте:
```
https://voice-chat-app-production-deba.up.railway.app
```

### 3.2. Что должно появиться:

Должен появиться JSON:
```json
{
  "status": "online",
  "message": "Voice Chat Server is running",
  "rooms": 0
}
```

**Если видите это** - сервер работает! ✅

**Если не видите** - проверьте логи на Railway или подождите еще минуту.

---

## Шаг 4: Протестируйте приложение

### 4.1. Запустите приложение

В PowerShell:
```powershell
cd C:\Users\Adam\Downloads\гс
npm start
```

Или запустите EXE файл (если уже собрали).

### 4.2. Создайте комнату

1. Введите ваше имя (например: "Адам")
2. Нажмите **"Создать новую комнату"**
3. Скопируйте ID комнаты (например: `abc123`)

### 4.3. Проверьте подключение

- Должно появиться сообщение: **"Вы успешно присоединились к комнате!"**
- Статус должен быть: **"Подключено"** (зеленый)
- Должен запросить доступ к микрофону

### 4.4. Протестируйте с другом

1. Поделитесь **ID комнаты** с другом
2. Друг запускает приложение
3. Друг вводит **только ID комнаты** (не нужно вводить IP!)
4. Друг нажимает "Присоединиться"
5. Вы должны слышать друг друга! 🎉

---

## ✅ Готово!

Если всё работает:
- ✅ Сервер отвечает в браузере
- ✅ Приложение подключается
- ✅ Можно создавать комнаты
- ✅ Друзья могут присоединяться

**Теперь можно пересобрать EXE для друзей:**
```powershell
npm run build-win
```

---

## 🐛 Если что-то не работает:

### Проблема: Git push не работает
- Проверьте, что вы авторизованы в GitHub
- Используйте Personal Access Token вместо пароля

### Проблема: Сервер не отвечает
- Проверьте логи на Railway
- Убедитесь, что статус "Running"
- Проверьте Start Command = `node server-cloud.js`

### Проблема: Приложение не подключается
- Откройте консоль (F12) и посмотрите ошибки
- Проверьте, что адрес сервера правильный в `public/app.js`

---

## 📝 Быстрая шпаргалка команд:

```powershell
# 1. Перейти в папку
cd C:\Users\Adam\Downloads\гс

# 2. Добавить файл
git add server-cloud.js

# 3. Коммит
git commit -m "Fix rooms variable"

# 4. Загрузить
git push

# 5. Запустить приложение
npm start
```


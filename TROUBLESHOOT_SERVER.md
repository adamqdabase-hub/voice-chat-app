# 🔧 Устранение проблемы с сервером

## Проблема: "Не удалось подключиться к серверу"

Если видите эту ошибку, проверьте следующее:

---

## Шаг 1: Проверьте статус сервера на Railway

1. Перейдите на Railway
2. Откройте ваш сервис "voice-chat-app"
3. Проверьте статус - должно быть **"Running"** (зеленая галочка)
4. Если статус "Crashed" или "Building" - подождите или проверьте логи

---

## Шаг 2: Проверьте логи на Railway

1. В Railway перейдите на вкладку **"Logs"**
2. Посмотрите последние сообщения
3. Должно быть:
   ```
   Сервер запущен на порту 3000
   Готов к подключениям!
   ```

Если видите ошибки - скопируйте их.

---

## Шаг 3: Проверьте Start Command

1. В Railway → Settings → Deploy
2. Убедитесь, что **Start Command** установлен:
   ```
   node server-cloud.js
   ```
3. Если нет - измените и сохраните

---

## Шаг 4: Проверьте работу сервера напрямую

Откройте в браузере:
```
https://voice-chat-app-production-deba.up.railway.app
```

Должно появиться JSON:
```json
{
  "status": "online",
  "message": "Voice Chat Server is running",
  "rooms": 0
}
```

**Если видите это** - сервер работает, проблема в клиенте.

**Если не видите** - проблема в сервере, проверьте логи.

---

## Шаг 5: Проверьте CORS и Socket.io

Если сервер работает (видите JSON), но клиент не подключается:

1. Убедитесь, что в `server-cloud.js` настроен CORS:
   ```javascript
   app.use(cors());
   ```

2. Проверьте, что Socket.io настроен правильно:
   ```javascript
   const io = socketIo(server, {
     cors: {
       origin: "*",
       methods: ["GET", "POST"]
     }
   });
   ```

---

## Шаг 6: Перезапустите сервис

В Railway:
1. Settings → Deploy
2. Нажмите "Redeploy" или "Restart"
3. Дождитесь завершения

---

## Частые проблемы:

**Проблема:** Сервер не запускается
- **Решение:** Проверьте Start Command = `node server-cloud.js`

**Проблема:** Порт не указан
- **Решение:** Railway автоматически устанавливает PORT через переменную окружения

**Проблема:** Ошибки в логах
- **Решение:** Скопируйте ошибки и проверьте код

---

## Быстрая проверка:

1. Railway → Logs → видите "Сервер запущен"?
2. Браузер → адрес сервера → видите JSON?
3. Если оба "да" - проблема в клиенте
4. Если "нет" - проблема в сервере, проверьте логи




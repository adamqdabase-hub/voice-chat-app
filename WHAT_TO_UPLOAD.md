# 📦 Что загружать в GitHub репозиторий

## ✅ НУЖНО загрузить:

### Основные файлы проекта:
- ✅ `server-cloud.js` - **ОБЯЗАТЕЛЬНО** (облачный сервер)
- ✅ `package.json` - **ОБЯЗАТЕЛЬНО** (зависимости)
- ✅ `.gitignore` - **ОБЯЗАТЕЛЬНО** (чтобы не загружать лишнее)
- ✅ `README.md` - описание проекта
- ✅ `public/` - папка с клиентской частью
  - `public/index.html`
  - `public/app.js`
  - `public/style.css`

### Документация (опционально):
- ✅ `CLOUD_DEPLOY.md`
- ✅ `HOW_TO_USE.md`
- ✅ `INTERNET_SETUP.md`
- ✅ Другие .md файлы

---

## ❌ НЕ нужно загружать:

### Автоматически генерируемые:
- ❌ `node_modules/` - установится автоматически на сервере
- ❌ `dist/` - папка с собранными EXE файлами
- ❌ `build/` - временные файлы сборки

### Локальные файлы:
- ❌ `main.js` - только для локального Electron приложения
- ❌ `.env` - локальные настройки (если есть)
- ❌ `*.log` - логи

### Личные файлы:
- ❌ Файлы с паролями/ключами
- ❌ Временные файлы

---

## 🎯 Минимальный набор для деплоя:

Для работы на Railway/Render нужны **только эти файлы:**

1. `server-cloud.js` ⭐
2. `package.json` ⭐
3. `.gitignore`
4. `public/` (если нужен веб-интерфейс)

**Всё остальное опционально!**

---

## 📝 Что делает .gitignore:

Файл `.gitignore` уже настроен и автоматически исключит:
- `node_modules/` - не загрузится
- `dist/` - не загрузится
- `*.log` - не загрузится

Так что можете просто сделать `git add .` - лишнее не загрузится!

---

## ✅ Итого:

**Загружайте весь проект** - `.gitignore` сам отфильтрует ненужное!

Просто выполните:
```powershell
git add .
git commit -m "Initial commit"
git push
```

Всё что нужно - загрузится, всё что не нужно - проигнорируется! 🎉

